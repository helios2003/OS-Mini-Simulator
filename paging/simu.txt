<html>

<head>
    <title>Address Calculation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }

        h1 {
            text-align: center;
        }

        .container {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .table-container {
            margin: 20px;
        }

        table {
            border-collapse: collapse;
        }

        th,
        td {
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #f2f2f2;
        }

        .input-container {
            margin-bottom: 10px;
        }

        .input-container input[type="text"] {
            padding: 5px;
            width: 100px;
        }

        .button-container {
            text-align: center;
            margin-top: 10px;
        }

        #physical-address {
            font-weight: bold;
        }

        .error-message {
            color: red;
            text-align: center;
        }
    </style>
</head>

<body>
    <h1>Address Calculation</h1>
    <div class="container">
        <div class="table-container">
            <h2>Page Table</h2>
            <table id="vtable" border="1">
                <tr>
                    <th>Page number</th>
                    <th>Data</th>
                </tr>
            </table>
        </div>

        <div class="table-container">
            <h2>Frame Table</h2>
            <table id="f" border="1">
                <tr>
                    <th>Page number</th>
                    <th>Frame number</th>
                </tr>
            </table>
        </div>

        <div class="table-container">
            <h2>Frames</h2>
            <table id="r" border="1">
                <tr>
                    <th>Frame number</th>
                    <th>Data</th>
                </tr>
            </table>
        </div>
    </div>

    <div class="input-container">
        Page Size: <input type="text" id="psize">
        Number of Pages: <input type="text" id="nump">
        Number of Frames: <input type="text" id="numf">
    </div>

    <div class="button-container">
        <button onclick="start();" id="b1">ADD DATA</button>
    </div>

    <div class="input-container">
        Enter Logical Address: <input type="text" id="logmem">
        <button onclick="calc();" value="Calculate">Calculate</button>
    </div>

    <div class="button-container">
        <button onclick="process()" id="b2">Submit</button>
    </div>

    <div class="error-message" id="physical-address"></div>

    <script>
        var log = [];
        var framet = [];
        var pt = [];
        var numpages = null;
        var numframes = null;
        var pagesize = null;

        function start() {
            var logical = document.getElementById("vtable");
            var pagetable = document.getElementById("f");
            var frames = document.getElementById("r");
            numpages = parseInt(document.getElementById("nump").value);
            numframes = parseInt(document.getElementById("numf").value);
            pagesize = parseInt(document.getElementById("psize").value);

            for (var i = 1; i <= numpages; i++) {
                var row = logical.insertRow(i);
                var cell1 = row.insertCell(0);
                var cell2 = row.insertCell(1);
                cell1.innerHTML = i - 1;
                var hue = "#" + ((1 << 24) * Math.random() | 0).toString(16);
                cell2.style.backgroundColor = hue;

                var row = pagetable.insertRow(i);
                var cell1 = row.insertCell(0);
                var cell2 = row.insertCell(1);
                cell1.innerHTML = i - 1;
                cell2.innerHTML = "<input type=text></input>"
                log.push(hue);
                pt.push(-1);

                var inp1 = document.getElementById("psize");
                var inp2 = document.getElementById("nump");
                var inp3 = document.getElementById("numf");
                var b1 = document.getElementById("b1");
                inp1.disabled = true;
                inp2.disabled = true;
                inp3.disabled = true;
                b1.disabled = true;
            }

            for (var i = 1; i <= numframes; i++) {
                var row = frames.insertRow(i);
                var cell1 = row.insertCell(0);
                var cell2 = row.insertCell(1);
                cell1.innerHTML = i - 1;
            }
        }

        function process() {
            var pagetable = document.getElementById("f");
            var frames = document.getElementById("r");

            for (var i = 1; i < numpages; i++) {
                if (pagetable.rows[i].cells[1].getElementsByTagName("input")[0].value.length > 0) {
                    pt[i - 1] = parseInt(pagetable.rows[i].cells[1].getElementsByTagName("input")[0].value);
                    frames.rows[pt[i - 1] + 1].cells[1].style.backgroundColor = log[i - 1];
                }
            }
        }

        function calc() {
            var b2 = document.getElementById("b2");
            b2.disabled = true;
            var pagetable = document.getElementById("f");
            var l = parseInt(document.getElementById("logmem").value);
            var pagenum = Math.floor(l / pagesize);
            var offset = l % pagesize;

            if (pt[pagenum] != -1) {
                var frame = pt[pagenum];
                var address = frame * pagesize + offset;
                document.getElementById("physical-address").innerHTML = "Physical Address: " + address;
            } else {
                document.getElementById("physical-address").innerHTML = "Not in frame table.";
            }
        }
    </script>
</body>

</html>